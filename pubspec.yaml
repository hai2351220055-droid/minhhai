name: minhhai
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2 # lưu use (lỗi thì xóa thèn này)
  cupertino_icons: ^1.0.8
  sqflite: ^2.3.0     # Thư viện SQLite
  path: ^1.8.0        # Dùng để lấy đường dẫn DB
  audioplayers: ^6.0.0 # Thư viện phát nhạc


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/ 
    - assets/music/




 